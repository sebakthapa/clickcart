import Image from "next/image"

const HeroImage = ({ src, isHomePage = true, }) => {

    return (
        <div className="heroImage-container">
            

        </div>
    )
}

export default HeroImage
